export const fontSizes = {
    text : {
        xs: 10,    // Captions, labels (e.g., timestamps)
        s: 12,    // Small body text (e.g., comments)
        m: 14,  // DEFAULT (Body text, buttons)
        l: 16,    // Lead text (e.g., intro paragraphs)
        xl: 18,    // Rare (e.g., emphasized text)
    },
    heading : {
        xs: 14,
        s: 16,
        m: 18,
        l: 20,
        xl: 22,
    },
    sub:{
        xs: 8,
        s: 10,
        m: 12,
        l: 14,
        xl: 16,
    }
}